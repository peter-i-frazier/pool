1. compare_maxP_vs_naive.R: generate recommendations using maxP, naive, mutate methods, and save to 'Benchmark.RData'

2. To generate plot, first run processDataForPlot.R, which write prob_of_shortest_hit to forplot.csv.
	Then run matlabPlot.m, which generate maxP_comparison.png.

3. maxP_recommendation.csv & naive_recommendation.csv are recommendations generated by maxP & naive methods.